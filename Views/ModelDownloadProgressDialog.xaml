<Window x:Class="AIChat.Views.ModelDownloadProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIChat.Views"
        mc:Ignorable="d"
        Title="下载模型" Height="200" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 模型名称 -->
        <TextBlock Grid.Row="0" x:Name="ModelNameTextBlock" Text="正在下载: " 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- 下载进度条 -->
        <ProgressBar Grid.Row="1" x:Name="DownloadProgressBar" Height="20" 
                     Minimum="0" Maximum="100" Value="0" Margin="0,0,0,10"/>

        <!-- 进度文本 -->
        <TextBlock Grid.Row="2" x:Name="ProgressTextBlock" Text="0%" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- 下载信息 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock x:Name="DownloadedSizeTextBlock" Text="已下载: 0 MB"/>
            <TextBlock Text=" / " Margin="5,0"/>
            <TextBlock x:Name="TotalSizeTextBlock" Text="总大小: 0 MB"/>
            <TextBlock Text=" | " Margin="10,0,5,0"/>
            <TextBlock x:Name="SpeedTextBlock" Text="速度: 0 KB/s"/>
            <TextBlock Text=" | " Margin="10,0,5,0"/>
            <TextBlock x:Name="TimeLeftTextBlock" Text="剩余时间: --"/>
        </StackPanel>

        <!-- 取消按钮 -->
        <Button Grid.Row="4" x:Name="CancelButton" Content="取消" 
                Width="80" Height="30" HorizontalAlignment="Center" 
                VerticalAlignment="Bottom" Margin="0,20,0,0"
                Background="#F44336" Foreground="White" BorderThickness="0" 
                Cursor="Hand" Click="CancelButton_Click"/>
    </Grid>
</Window>
